# 🍳 KitchenPro Frontend

React приложение для управления кухней с интуитивным пользовательским интерфейсом.

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 📋 Доступные скрипты

- `npm run dev` - Запуск в режиме разработки с hot reload
- `npm run build` - Сборка для production
- `npm run preview` - Просмотр production сборки

## 📁 Структура папок

```
src/
├── components/      # React компоненты
├── pages/           # Страницы приложения
├── services/        # API клиент
├── store/           # Zustand stores (состояние)
├── types/           # TypeScript интерфейсы
├── utils/           # Утилиты
├── App.tsx          # Главный компонент
├── main.tsx         # Точка входа
└── index.css        # Глобальные стили
```

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Строгая типизация
- **React Router v6** - Навигация
- **Zustand** - Управление состоянием
- **Tailwind CSS** - Стилизация
- **Vite** - Fast build tool
- **Lucide React** - Иконки
- **Axios** - HTTP клиент

## 🔗 Интеграция с Backend

Приложение подключается к backend API на `http://localhost:5000`.

Все API запросы обрабатываются в `src/services/api.ts`.

## 💾 Состояние приложения

Используется Zustand для управления состоянием:

- `useDashboardStore` - Состояние dashboard
- `useAuthStore` - Состояние аутентификации

## 📋 Страницы

- `/` - Dashboard (главная панель)
- `/inventory` - Управление складом
- `/recipes` - Меню и рецепты
- `/orders` - Управление заказами

## 🔐 Аутентификация

Token сохраняется в localStorage и автоматически отправляется при каждом запросе.

## 📚 Дополнительная информация

- Главный README: [README.md](../README.md)
- API документация: [API_DOCS.md](../docs/API_DOCS.md)
- Развертывание: [DEPLOYMENT.md](../docs/DEPLOYMENT.md)
