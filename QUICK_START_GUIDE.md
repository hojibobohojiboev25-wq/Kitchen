# 🍳 KitchenPro -완벽한 설정 사항 확인

모든 것이 준비되었습니다! 아래 단계를 따라 애플리케이션을 실행하세요.

---

## 🚀 **빠른 시작 (추천)**

### **Windows 사용자:**

1. 파일 탐색기에서 `C:\Users\Asus\Desktop\KitchenPro` 폴더 열기
2. `START.bat` 파일을 **더블클릭** (또는 마우스 우클릭 → 열기)
3. **약 15초 기다리기** (백엔드 + 프론트엔드 시작)
4. 브라우저가 자동으로 `http://localhost:3000` 열림

### **Linux/Mac 사용자:**

```bash
cd ~/Desktop/KitchenPro
chmod +x start-all.sh
./start-all.sh
```

---

## 📋 **로그인 정보**

```
이메일: demo@kitchenpro.com
비밀번호: demo123
```

---

## ✅ **확인 목록**

이것이 보여야 할 것입니다:

### **백엔드 (터미널 1)**
```
🍳 KitchenPro server running on port 5000
✓ Database connected (또는 ⚠ Database connection failed - 괜찮음)
```

### **프론트엔드 (터미널 2)**
```
VITE v5.0.8  ready in XXX ms
Local:    http://localhost:3000/
```

### **브라우저 (http://localhost:3000)**
- 로그인 페이지가 표시됨 ✅
- 이메일/비밀번호 입력 후 로그인 가능 ✅
- Dashboard 페이지로 이동 ✅

---

## 🛠️ **수동 시작 (문제 있을 경우)**

### **터미널 1 - 백엔드:**
```powershell
cd backend
npm run dev
```

### **터미널 2 - 프론트엔드:**
```powershell
cd frontend
npm run dev
```

---

## ❌ **문제 해결**

### **포트 이미 사용 중**
```powershell
netstat -ano | findstr :5000
taskkill /PID [숫자] /F
```

### **404 에러**
- 백엔드가 실행 중인지 확인: `http://localhost:5000/health`
- CORS 설정 확인

### **node_modules 오류**
```powershell
cd backend && npm install
cd ../frontend && npm install
```

---

## 🌐 **Vercel 배포**

모든 구성이 준비되었습니다:
- ✅ Backend vercel.json 설정됨
- ✅ Frontend vercel.json 설정됨
- ✅ 환경 변수 구성됨

배포만 하면 됩니다!

```bash
vercel deploy
```

---

## 📚 **추가 명령어**

```bash
# 전체 설정 (의존성 설치)
npm run install-all

# 프로덕션 빌드
npm run build

# 테스트 실행
npm run test

# 린트 확인
npm run lint
```

---

## 🎉 **완료!**

이제 준비되었습니다. `START.bat`을 실행하고 즐기세요! 🍳

문제가 있으면 터미널 에러 메시지를 공유해주세요.
