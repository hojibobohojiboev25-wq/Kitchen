# 🍳 KitchenPro - Система управления кухней

Полный контроль кухни в одном месте. Комплексная система управления кухней для ресторанов и кафе.

## 🎯 Основные возможности

### 📊 Главная панель (Dashboard)
- Полный контроль кухни в одном месте
- Видимость товаров, которые заканчиваются
- Список заказов поставщикам
- Блюда дня
- Информация о том, кто сегодня на смене
- Себестоимость дня
- Прибыль дня

### 📦 Управление складом (Inventory System)
- Учёт остатков в кг / литрах / штуках
- Автоматическое списание при продаже блюда
- Уведомление: "Заканчивается"
- История движения продукта
- Фото + срок годности
- Контроль списания (порча, брак)
- Автоматический расчет количества продуктов на нужное количество порций

### 📋 Меню и рецепты (Recipe Engine)
Каждое блюдо содержит:
- Фото
- Точный рецепт
- Технологическую карту
- Себестоимость
- Аллергены
- Пищевую ценность
- Автоматический расчёт на 10 / 50 / 100 гостей
- Автоматическое пересчитывание себестоимости при изменении цены продукта

### 🧾 Управление закупками (Purchasing)
- База поставщиков
- История цен
- Сравнение цен
- Автоматический заказ
- PDF экспорт заказа
- Контроль поставки

### 🧑‍🍳 Управление персоналом
- График смен
- Кто отвечает за что
- Учёт рабочего времени
- KPI поваров
- Контроль производительности

### 🍽️ Kitchen Display System (для кухни)
- Экран на кухне с заказами в реальном времени
- Таймер приготовления
- Приоритет блюд
- Статус: готовится / готово
- Без бумажек

### 📊 Финансы и аналитика
- Себестоимость блюд
- Маржа
- Самые прибыльные блюда
- Самые убыточные
- Потери продуктов
- Прогноз закупок
- AI прогнозирование спроса

---

## 🏗️ Технический стек

### Backend
- **Node.js** + **Express.js** - REST API сервер
- **TypeScript** - строгая типизация
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация
- **Multer** - загрузка файлов

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - строгая типизация
- **React Router** - навигация
- **Zustand** - управление состоянием
- **Tailwind CSS** - стилизация
- **Vite** - быстрая сборка
- **Recharts** - графики и диаграммы
- **Lucide React** - иконки

### Database
- **PostgreSQL** - полнофункциональная реляционная БД
- **UUID** для уникальных идентификаторов

---

## 📋 Предварительные требования

- Node.js 16+ и npm/yarn
- PostgreSQL 12+
- Git

---

## 🚀 Установка и запуск

### 1️⃣ Клонирование репозитория

```bash
cd KitchenPro
```

### 2️⃣ Установка Backend

```bash
cd backend
npm install
cp .env.example .env
```

**Отредактируйте `.env` файл с вашими настройками PostgreSQL:**

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kitchenpro
DB_USER=postgres
DB_PASSWORD=your_password
JWT_SECRET=your_super_secret_jwt_key
```

### 3️⃣ Настройка базы данных

Создайте базу данных:

```bash
createdb kitchenpro
```

Применитеstartegy, запустив SQL миграции:

```bash
psql -U postgres -d kitchenpro -f ../database/schema.sql
```

### 4️⃣ Запуск Backend сервера

```bash
npm run dev
```

Backend будет запущен на `http://localhost:5000`

### 5️⃣ Установка Frontend

В новом терминале:

```bash
cd frontend
npm install
```

### 6️⃣ Запуск Frontend

```bash
npm run dev
```

Frontend будет запущен на `http://localhost:3000`

---

## 📁 Структура проекта

```
KitchenPro/
├── backend/                 # Express.js сервер
│   ├── src/
│   │   ├── config/         # Конфигурация (БД, JWT)
│   │   ├── controllers/    # Логика обработки запросов
│   │   ├── middleware/     # Middleware (auth, error)
│   │   ├── routes/         # Маршруты API
│   │   ├── services/       # Бизнес-логика
│   │   ├── types/          # TypeScript типы
│   │   ├── utils/          # Утилиты
│   │   └── index.ts        # Главная точка входа
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                # React приложение
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы приложения
│   │   ├── services/       # API клиент
│   │   ├── store/          # Zustand stores
│   │   ├── types/          # TypeScript типы
│   │   ├── utils/          # Утилиты
│   │   ├── App.tsx         # Главный компонент
│   │   └── main.tsx        # Точка входа
│   ├── package.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── tsconfig.json
│
├── database/
│   └── schema.sql          # SQL схема БД
│
└── docs/                   # Документация
```

---

## 📚 API Endpoints

### Рецепты
- `GET /api/recipes` - Получить все рецепты
- `GET /api/recipes/:id` - Получить рецепт по ID
- `POST /api/recipes` - Создать новый рецепт (требует auth)
- `PUT /api/recipes/:id` - Обновить рецепт (требует auth)
- `DELETE /api/recipes/:id` - Удалить рецепт (требует auth)
- `POST /api/recipes/:id/scale` - Масштабировать рецепт на N порций

### Инвентарь
- `GET /api/inventory` - Получить все товары
- `GET /api/inventory/low-stock` - Товары, заканчивающиеся
- `GET /api/inventory/:id` - Получить товар по ID
- `POST /api/inventory` - Создать товар (требует auth)
- `PUT /api/inventory/:id` - Обновить товар (требует auth)
- `POST /api/inventory/:id/deduct` - Вычесть количество товара
- `DELETE /api/inventory/:id` - Удалить товар (требует auth)

### Заказы
- `GET /api/orders` - Получить все заказы
- `GET /api/orders/pending` - Получить активные заказы
- `GET /api/orders/:id` - Получить заказ по ID
- `POST /api/orders` - Создать заказ
- `PUT /api/orders/:id/status` - Обновить статус заказа
- `PUT /api/orders/:id/complete` - Завершить заказ

### Аналитика
- `GET /api/analytics/dashboard` - Получить метрики dashboard
- `GET /api/analytics/recipes/profitability` - Анализ прибыльности блюд
- `GET /api/analytics/suppliers` - Анализ поставщиков
- `GET /api/analytics/sales/report` - Отчет о продажах (последние 30 дней)
- `GET /api/analytics/inventory/value` - Стоимость товаров на складе

---

## 🔐 Аутентификация

Приложение использует JWT для аутентификации. Токен должен передаваться в header:

```
Authorization: Bearer <token>
```

Роли пользователей:
- **admin** - Полный доступ
- **manager** - Управление рецептами, складом, финансами
- **chef** - Просмотр и обновление заказов
- **viewer** - Только просмотр

---

## 🛠️ Разработка

### Hot Reload
- Backend: Используется `ts-node-dev` для автоматической перезагрузки при изменении кода
- Frontend: Vite обеспечивает мгновенный Hot Module Replacement (HMR)

### Тестирование Backend
```bash
cd backend
npm test
```

### Сборка на Production

**Backend:**
```bash
cd backend
npm run build
npm start
```

**Frontend:**
```bash
cd frontend
npm run build
```

---

## 📖 Документация API

Полная документация доступна в файле [API_DOCS.md](./docs/API_DOCS.md)

---

## 🤝 Вклад в проект

1. Создайте feature branch (`git checkout -b feature/amazing-feature`)
2. Commintit изменения (`git commit -m 'Add amazing feature'`)
3. Push в branch (`git push origin feature/amazing-feature`)
4. Открыть Pull Request

---

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для дополнительной информации.

---

## 🤔 Часто задаваемые вопросы

### Как добавить новую роль пользователя?
1. Обновить тип `role` в [backend/src/types/index.ts]
2. Использовать middleware `requireRole()` для ограничения доступа

### Как настроить облачное хранилище?
1. Установить пакет для облачного сервиса (например, `aws-sdk`)
2. Обновить конфиг и сервис загрузки файлов

### Как интегрировать AI для прогнозирования?
1. Добавить интеграцию с ML API (например, OpenAI)
2. Создать сервис для предсказания спроса
3. Интегрировать в analyticsService

---

## 💬 Поддержка

Если у вас есть вопросы или проблемы, пожалуйста откройте Issue в репозитории.

---

**Спасибо за использование KitchenPro!** 🎉

Приятного использования системы управления кухней! 🍳
