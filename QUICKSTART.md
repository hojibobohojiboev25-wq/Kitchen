# 🚀 KitchenPro - Быстрый Старт

Добро пожаловать в KitchenPro - полнофункциональную систему управления кухней!

## ⚡ Требования (проверьте наличие)

- [ ] **Node.js** 18+ установлен (`node --version`)
- [ ] **npm** 9+ установлен (`npm --version`)
- [ ] **PostgreSQL** 12+ установлен или Docker доступен
- [ ] **VS Code** или другой редактор

Если что-то не установлено, см. [POSTGRES_SETUP_WINDOWS.md](./POSTGRES_SETUP_WINDOWS.md)

## ⚡ Быстрый старт (5 минут)

### Шаг 1: Убедитесь, что PostgreSQL запущен

Проверьте наличие PostgreSQL:
```powershell
psql --version
```

Если ошибка, установите PostgreSQL: https://www.postgresql.org/download/windows/

**Или используйте Docker:**
```powershell
docker run --name kitchenpro-db -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:16
```

### Шаг 2: Инициализируйте базу данных

```powershell
# Перейдите в корневую папку KitchenPro
cd c:\Users\Asus\Desktop\KitchenPro

# Инициализируйте БД (создаёт базу и применяет схему)
node database/init.js
```

Ожидаемый результат:
```
✅ PostgreSQL найден
✅ База данных готова
✅ Схема инициализирована успешно!
✅ База данных соединена!
```

### Шаг 3: Запустите бэкенд (API сервер)

```powershell
cd backend
npm run dev
```

Ожидаемый результат:
```
[INFO] Server is running on http://localhost:5000
[INFO] Database connected successfully
```

### Шаг 4: В новом PowerShell окне, запустите фронтенд (веб-приложение)

```powershell
cd frontend
npm run dev
```

Ожидаемый результат:
```
Local: http://localhost:3000/
```

### Шаг 5: Откройте приложение в браузере

1. Откройте http://localhost:3000
2. Введите учётные данные:
   - **Email:** admin@kitchenpro.local
   - **Пароль:** admin123
3. Нажмите "Login"

🎉 Готово! Вы вошли в приложение!

---

## 📊 Основные функции

### Dashboard 📊
Главная страница с метриками:
- Выручка за день
- Активные заказы
- Все продукты в системе
- Низкий уровень запасов
- Быстрые действия

### Inventory 📦
Управление товарами:
- Таблица со всеми продуктами
- Статус наличия (есть, мало, нет)
- Цена за единицу
- Всего на складе
- Добавить/отредактировать товар

### Recipes 🍽️
Управление рецептами:
- База всех блюд
- Ингредиенты и количество
- Себестоимость блюда
- Рекомендованная цена
- Создать новый рецепт

### Orders 📧
Управление заказами:
- Список активных заказов
- Статусы: новый, в приготовлении, готово
- История заказов
- Создать новый заказ

### Analytics 📈 (по требованию)
Отчеты и статистика:
- Продажи по дням
- Популярные блюда
- Прибыльность меню
- Отчеты по сезонам

---

## 🔧 Доступные команды

### Backend (в папке `backend/`)

```powershell
npm run dev       # Запуск с автоперезагрузкой (разработка)
npm run build     # Компиляция TypeScript в JavaScript
npm start         # Запуск скомпилированного кода
npm test          # Запуск тестов
npm run db:init   # Инициализация БД (если нужно снова)
```

### Frontend (в папке `frontend/`)

```powershell
npm run dev       # Запуск с горячей перезагрузкой (разработка)
npm run build     # Сборка для production
npm run preview   # Просмотр production сборки локально
npm run lint      # Проверка качества кода
```

### Root (корневая папка KitchenPro)

```powershell
node database/init.js    # Инициализация базы данных
.\setup-windows.bat      # Автоматический запуск (только для Windows)
```

---

## 👥 Учётные данные для тестирования

Встроенные аккаунты в системе:

| Роль | Email | Пароль | Доступ |
|------|-------|--------|--------|
| **Администратор** | admin@kitchenpro.local | admin123 | Все функции |
| **Менеджер** | manager@kitchenpro.local | manager123 | Заказы, Инвентарь, Рецепты |
| **Повар** | chef@kitchenpro.local | chef123 | Заказы, Рецепты |
| **Просмотр** | viewer@kitchenpro.local | viewer123 | Только просмотр (Dashboard) |

---

## 🚨 Решение проблем

### ❌ Ошибка: "psql: command not found"
**Решение:** PostgreSQL не установлен или не в PATH
```powershell
# Проверьте установку
psql --version

# Если ошибка - установите PostgreSQL:
# https://www.postgresql.org/download/windows/

# После установки добавьте в PATH:
# C:\Program Files\PostgreSQL\16\bin
```

### ❌ Ошибка: "Port 5000 already in use"
**Решение:** Другой процесс использует порт 5000
```powershell
# Измените PORT в backend/.env:
PORT=5001

# Или убейте процесс на порту 5000
Get-Process -Id (Get-NetTCPConnection -LocalPort 5000).OwningProcess | Stop-Process
```

### ❌ Ошибка: "Connection refused (localhost:5432)"
**Решение:** PostgreSQL не запущен
```powershell
# Проверьте, запущен ли PostgreSQL
# Windows Services (Win + R → services.msc)
# Найдите "postgresql-x64-**" и убедитесь, что статус "Running"

# Или запустите в контейнере Docker:
docker run --name kitchenpro-db -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:16
```

### ❌ Ошибка: "npm ERR! 404 Not Found"
**Решение:** Зависимость не найдена - пересоздайте модули
```powershell
cd backend  # или frontend
rm -Force -Recurse node_modules
npm install
```

### ❌ Ошибка: "Cannot GET /"
**Решение:** Фронтенд сервер не запущен
```powershell
cd frontend
npm run dev
```

Для всех ошибок см. [POSTGRES_SETUP_WINDOWS.md](./POSTGRES_SETUP_WINDOWS.md) и [документацию](./docs)

---

## 📁 Структура проекта

```
KitchenPro/
├── backend/        # REST API сервер
├── frontend/       # React приложение
├── database/       # SQL схемы
├── docs/           # Документация
└── README.md       # Главная документация
```

## 💡 Основные компоненты

### Dashboard 📊
Главная страница с метриками:
- Себестоимость дня
- Прибыль
- Активные заказы
- Товары с низким уровнем

### Inventory 📦
Управление товарами:
- Просмотр остатков
- Добавление/редактирование товаров
- Отслеживание категорий

### Recipes 🍽️
Управление рецептами:
- Создание блюд
- Расчет себестоимости
- Масштабирование на N порций

### Orders 📧
Управление заказами:
- Просмотр активных заказов
- Обновление статуса
- Управление приоритетом

## 👥 Ролевая система

- **admin** - Полный доступ ко всему
- **manager** - Управление рецептами, финансами
- **chef** - Просмотр и готовка заказов
- **viewer** - Только просмотр

## 🚀 Следующие шаги

1. **Уточните конфигурацию** - Отредактируйте `.env` под ваши нужды
2. **Добавьте данные** - Заполните товары, рецепты, поставщиков
3. **Интегрируйте POS** - Подключите вашу POS систему (опционально)
4. **Учите команду** - Обучите персонал использованию системы
5. **Деплойте** - Развертните на production сервер

## 💬 Получить помощь

- 📖 Прочитайте [полную документацию](./README.md)
- 🐛 Откройте issue на GitHub
- 💬 Проверьте FAQ в документации

---

Удачи с KitchenPro! Надеемся, что система будет полезна для вашей кухни! 🍳✨
